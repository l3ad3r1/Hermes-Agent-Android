<?xml version="1.0" encoding="utf-8"?>
<!--
    Hermes Agent — AndroidManifest
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- app_launch resolves only user-visible launcher activities. Android 11+
         otherwise hides installed apps from PackageManager queries. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.termux" />
        <package android:name="moe.shizuku.privileged.api" />
    </queries>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />

    <!-- Always-on agent: foreground service (24/7) + restart on boot. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- In-app OTA updater: install the downloaded release APK without a browser. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- On-device LLM models: download GGUF files into a user-visible top-level
         "AI Models" folder (or any folder the user names). A true top-level folder
         on shared storage requires All-Files access on Android 11+; granted at
         runtime via Settings (Environment.isExternalStorageManager). -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Android 10 (API 29) has no All-Files access; it uses legacy raw-path
         writes to shared storage via WRITE_EXTERNAL_STORAGE +
         requestLegacyExternalStorage on <application> below. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <!-- SMS gateway (sent via the notify tool / SmsManager). -->
    <uses-permission android:name="android.permission.SEND_SMS" />

    <!-- Capabilities the agent can use (requested at runtime). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Run commands in the separately-installed Termux app (RUN_COMMAND). -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- Shizuku (privileged shell execution via ADB UID 2000). -->
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- The Tasker fire receiver used to be guarded by a signature-level
         permission. That was airtight and useless: Tasker is signed by its own
         publisher, so the only app that could satisfy it was Hermes itself, and
         the integration could never work. Access is now gated by a capability
         token minted when the user approves a specific automation app by
         package AND signing certificate — see TaskerHostAuthority. -->

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:name=".HermesApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HermesAgent"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="34">

        <!-- Shizuku provider binding -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <activity
            android:name=".ui.share.ShareTargetActivity"
            android:exported="true"
            android:theme="@style/Theme.HermesAgent.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.HermesAgent">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Always-on agent service: keeps Hermes working the Kanban board across Doze. -->
        <service
            android:name=".service.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Local OpenAI-compatible API server (opt-in, Settings-controlled). -->
        <service
            android:name=".service.ApiServerService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Opt-in notification capture for the daily digest. -->
        <service
            android:name=".service.HermesNotificationListener"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Widgets -->
        <receiver android:name=".ui.widget.BriefingWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_briefing" />
        </receiver>

        <receiver android:name=".ui.widget.QuickJotWidgetProvider" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_quick_jot" />
        </receiver>

        <!-- Receivers -->
        <receiver android:name=".receiver.NotificationReplyReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.hermes.agent.action.REPLY" />
            </intent-filter>
        </receiver>

        <receiver android:name=".data.proactive.LessOfThisReceiver" android:exported="false">
            <intent-filter>
                <action android:name="com.hermes.agent.action.LESS_OF_THIS" />
            </intent-filter>
        </receiver>

        <!-- OTA download foreground service -->
        <service
            android:name=".data.update.OtaDownloadService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Long-running local-model downloads are owned by WorkManager and
             advertised as data-sync foreground work on Android 14+. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Quick Settings Tile -->
        <service
            android:name=".ui.tile.HermesQuickTileService"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".service.HermesVoiceInteractionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction" />
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </service>

        <service
            android:name=".service.HermesVoiceInteractionSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true" />

        <!-- AppAgent App Control via AccessibilityService -->
        <service
            android:name=".service.AppAgentAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Reconciles persisted queued work after credential-protected storage is available. -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Serves the downloaded update APK to the system installer (in-app OTA). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager initialization deferred to HermesApp via HiltWorkerFactory -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
            <!-- Starts MemoryPressureMonitor before the first screen; HermesApp.onCreate is the fallback. -->
            <meta-data
                android:name="com.hermes.agent.data.performance.MemoryMonitorInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- OAuth deep-link callback handler -->
        <activity
            android:name=".ui.oauth.OAuthCallbackActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="hermes"
                    android:host="oauth"
                    android:pathPrefix="/callback" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    android:scheme="https"
                    android:host="agent.hermes"
                    android:pathPrefix="/oauth/callback" />
            </intent-filter>
        </activity>

        <!-- Tasker / Locale Automation Plugin -->
        <activity
            android:name=".plugin.tasker.TaskerPluginActivity"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Hermes Agent">
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.EDIT_SETTING" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".plugin.tasker.TaskerPluginReceiver"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="Hermes Agent">
            <!-- Reachable by any app, and useless to one: TaskerPluginReceiver
                 drops every fire whose bundle does not carry a live token from
                 an approved host. -->
            <intent-filter>
                <action android:name="com.twofortyfouram.locale.intent.action.FIRE_SETTING" />
            </intent-filter>
        </receiver>

        <!-- Wake Word Foreground Service & Boot Receiver -->
        <service
            android:name=".service.WakeWordService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <receiver
            android:name=".service.WakeWordBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
